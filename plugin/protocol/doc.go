// Package protocol owns the public Theater plugin transport envelopes and
// method payloads.
//
// Use it when working close to the native plugin JSON-RPC transport or when
// implementing custom tooling around plugin sessions.
package protocol
